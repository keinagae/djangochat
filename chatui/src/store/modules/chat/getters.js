export const conversions = (state) => {
    return state.conversions
}

export const messages = (state) => {
    return state.messages
}

export const chatToken = (state) => {
    return state.chat_token
}

export const availableContacts = (state) => {
    return state.availableContacts
}

export const chatSocket = (state) => {
    return state.chatSocket
}

export const activeConversion = (state) => {
    return state.activeConversion
}